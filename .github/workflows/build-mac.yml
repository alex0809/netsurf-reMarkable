name: Build for reMarkable (macOS)
on: push
jobs:
  build:
    runs-on:  macos-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build Docker image
        run: make image
      - name: Build
        run: make build
